<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@android:color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:text">@string/app_name</item>
    </style>

    <style name="IntroTextField">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="IntroTextField.Heading" parent="IntroTextField">
        <item name="android:fontFamily">@font/grinty_font_family</item>
    </style>

    <style name="IntroTextField.Heading.Title" parent="IntroTextField.Heading">
        <item name="android:textSize">96sp</item>
    </style>

    <style name="IntroTextField.Heading.Subtitle" parent="IntroTextField.Heading">
        <item name="android:textSize">64sp</item>
    </style>

    <style name="IntroTextField.Tagline" parent="IntroTextField">
        <item name="android:paddingLeft">@dimen/desc_padding</item>
        <item name="android:paddingRight">@dimen/desc_padding</item>
        <item name="android:textSize">@dimen/slide_desc</item>
    </style>

    <style name="ToolbarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/colorBottomDark</item>
        <item name="android:textColorSecondary">@color/colorBottomDark</item>
        <item name="actionMenuTextColor">@color/colorBottomDark</item>
        <item name="actionOverflowButtonStyle">@style/Widget.AppCompat.Light.ActionButton.Overflow</item>
    </style>

</resources>
